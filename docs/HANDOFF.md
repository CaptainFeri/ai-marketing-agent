# Handoff — پلتفرم تولید محتوای SEO/GEO و چندرسانه‌ای

> وضعیت: **فاز ۱ (MVP چندمشتری) از نظر کد کامل شده.** هر بخشی که به وزن مدل یا
> سخت‌افزار GPU واقعی نیاز نداشت به‌صورت کاملاً واقعی پیاده شده و تست خودکار
> دارد؛ هر بخشی که به آن‌ها نیاز دارد (vLLM، FLUX، Wan2.2، Piper/Chatterbox،
> LatentSync) به‌صورت آشکار «Unavailable» یا «Simulated» است تا فاز ۰ روی
> سخت‌افزار واقعی آن‌ها را جایگزین کند — نه شبیه‌سازی پنهان. تنها بند باز فاز ۱
> راه‌اندازی مشتری‌های آزمایشی واقعی است (به بخش «هفته ۹ تا ۱۰» نگاه کنید).
> تاریخ آخرین به‌روزرسانی: ۳۰ شهریور ۱۴۰۵ (2026-09-21)
> تاریخ نگارش اولیه: ۲۹ شهریور ۱۴۰۵ (2026-09-20)
> این فایل خلاصه تصمیم‌ها و برنامه کامل فازهاست. هر جا با سند معماری تناقض داشت، سند معماری معتبر است.

---

## ۱. پروژه در یک پاراگراف

یک پلتفرم **SaaS چندمشتری** که خط تولید محتوای هفت‌مرحله‌ای را اجرا می‌کند:
- پرسشنامه برند را می‌شناسد.
- زنجیره‌ای از ایجنت‌ها متن SEO/GEO می‌نویسند.
- متن برای هر کانال بازاریابی‌محور می‌شود.
- بعد از **تایید انسانی** تصویر و ویدیو به‌صورت موازی تولید می‌شوند.
- **انسان** گزینه‌ها را انتخاب و زمان‌بندی می‌کند.
- محتوا منتشر می‌شود و عملکرد و رشد اندازه‌گیری و به چرخه بعد برگردانده می‌شود.

همه مدل‌ها روی یک **RTX 3090 Ti** (۲۴ گیگ VRAM) اجرا می‌شوند.

## ۲. تصمیم‌های قطعی

| # | تصمیم | توضیح |
|---|---|---|
| D1 | همه مدل‌ها **خودمیزبان** | هیچ فراخوانی به API مدل‌های خارجی نداریم و داده از سرور خارج نمی‌شود |
| D2 | **MVP بدون هزینه اشتراک/API** | گزینه‌های پولی فقط در فاز ۴ (بهبود) می‌آیند |
| D3 | زبان‌ها: **فارسی، انگلیسی، عربی** | متن، صدا و زیرنویس. کیورد هر زبان جدا تحقیق می‌شود (بومی‌سازی، نه ترجمه) |
| D4 | ویدیو **سه حالت** دارد: `face` / `voice` / `none` | پیش‌فرض در Brand Brief تعیین می‌شود و ویراستار برای هر بسته می‌تواند عوضش کند |
| D5 | ارکستراسیون: **Celery + Redis** | Temporal در فاز ۴ |
| D6 | فقط مدل‌هایی با **مجوز تجاری** | FLUX.1-dev، XTTS-v2، Wav2Lip و MMS-TTS ممنوع‌اند |
| D7 | چهره و صدای واقعی فقط با **رضایت‌نامه کتبی** | خروجی آواتار برچسب «ساخته‌شده با AI» می‌گیرد |
| D8 | **چندمشتری (Multi-tenant)** | از روز اول `tenant_id` در همه جدول‌ها وجود دارد و داده، فایل و اعتبارنامه‌های کانال هر مشتری ایزوله است |
| D9 | سخت‌افزار: **یک RTX 3090 Ti با ۲۴ گیگ حافظه** | مدل‌ها روی GPU نوبتی اجرا می‌شوند (بخش ۶) |
| D10 | **حجم تولید تابع ظرفیت** است | سیستم زمان GPU هر مرحله را اندازه می‌گیرد و سهمیه روزانه هر مشتری را از ظرفیت واقعی حساب می‌کند (بخش ۷) |

## ۳. جریان کار

```
۱ پرسشنامه → Brand Brief (JSON)
۲ پایپلاین متن: پژوهشگر → استراتژیست → نویسنده → بهینه‌ساز GEO → بهینه‌ساز SEO → QA
      (اگر QA امتیاز کم بدهد، حداکثر ۲ بار به نویسنده برمی‌گردد و بعد به انسان ارجاع می‌شود)
۳ بازاریابی‌سازی → نسخه هر کانال + visual_brief + نسخه‌های A/B
   ── دروازه انسانی ۱: تایید / بازگشت با بازخورد / رد ──
۴ تولید رسانه: صف تصویر ‖ صف ویدیو (روی یک GPU به‌صورت نوبتی)
   ── دروازه انسانی ۲: انتخاب گزینه + زمان‌بندی روی تقویم (شمسی/میلادی) ──
۶ انتشار: Scheduler → کانکتور هر کانال (سه بار تلاش مجدد، سپس هشدار به اپراتور)
۷ سنجش: آمار روزانه → داشبورد → حلقه بازخورد (Refresh برای رتبه ۸ تا ۲۰)
```

چرخه وضعیت ContentPackage:
`Planned → Drafting → TextReview → (Drafting | Rejected | MediaGenerating) → Selection → Scheduled → Published → Measuring → (Refresh → Drafting)`

## ۴. استک

| لایه | انتخاب |
|---|---|
| بک‌اند | Python + FastAPI |
| ایجنت‌ها | LangGraph یا کد ساده اختصاصی (خروجی هر ایجنت با JSON Schema اعتبارسنجی می‌شود) |
| صف/گردش کار | Celery + Redis. یک صف `gpu` با worker تک‌نخی (concurrency=1) و صف‌های CPU جدا |
| دیتابیس | PostgreSQL + pgvector، با Row-Level Security روی `tenant_id` |
| فایل | MinIO با پیشوند جدا برای هر مشتری |
| رمز اعتبارنامه‌ها | توکن‌های کانال رمزشده در دیتابیس (کلید در متغیر محیطی/فایل مخفی) |
| پنل | Next.js با پشتیبانی RTL/LTR |
| احراز هویت | حساب کاربری + نقش‌ها (مالک، مدیر، ویراستار، بیننده) در هر Workspace |
| مشاهده‌پذیری | Langfuse خودمیزبان + لاگ زمان GPU هر مرحله |
| استقرار | Docker Compose روی همان سرور |

## ۵. مدل‌ها (برای ۲۴ گیگ VRAM؛ در فاز ۰ آزموده شوند)

| کار | مدل | مجوز | VRAM تقریبی | محل اجرا |
|---|---|---|---|---|
| متن fa/en/ar | **Qwen3-30B-A3B** (MoE، کوانتایز ۴بیتی). جایگزین: Qwen3-14B یا Gemma 3 12B | Apache-2.0 / Gemma Terms | ~۱۸ گیگ | GPU (vLLM) |
| جاسازی | BGE-M3 | MIT | کم | CPU یا GPU |
| تصویر | FLUX.1-schnell (fp8) | Apache-2.0 | ~۱۲ تا ۱۶ گیگ | GPU (ComfyUI) |
| کلیپ ویدیو | **Wan 2.2 TI2V-5B** (نسخه ۱۴B روی ۲۴ گیگ فقط با offload و بسیار کند اجرا می‌شود) | Apache-2.0 | ~۲۰ تا ۲۴ گیگ | GPU (ComfyUI) |
| TTS فارسی | Piper (صداهای fa_IR) | مجوز هر صدا جدا بررسی شود | — | **CPU** |
| TTS انگلیسی/عربی | Chatterbox Multilingual | MIT | کم | GPU یا CPU |
| زیرنویس | Whisper large-v3 (faster-whisper) | MIT | ~۴ تا ۶ گیگ | GPU |
| لب‌خوانی از ویدیو | LatentSync 1.5 (سریع‌تر: MuseTalk) | Apache-2.0 / MIT | ~۸ گیگ | GPU |
| لب‌خوانی از عکس | SadTalker | Apache-2.0 | متوسط | GPU |

نکته‌ها:
- Chatterbox فارسی ندارد، پس مسیر صدای فارسی جداست.
- مدل‌های تصویر حروف فارسی و عربی را خراب می‌کنند. تصویر **بدون متن** ساخته می‌شود و متن با قالب HTML (Playwright) رویش رندر می‌شود.
- وزن مدل‌ها در یک مخزن داخلی با نسخه و مجوز نگهداری می‌شوند (آینه داخلی).
- حداقل **۶۴ گیگ RAM** سیستم لازم است (بهتر است ۱۲۸ گیگ باشد) تا بتوان وزن‌ها را موقتاً از GPU به RAM منتقل کرد. **NVMe** سرعت بارگذاری را بالا می‌برد.

## ۶. اجرای نوبتی روی یک GPU

LLM و مدل‌های رسانه‌ای هم‌زمان در ۲۴ گیگ جا نمی‌شوند. به همین دلیل یک **GPU Scheduler** مالک کارت است:

- **دو پنجره کاری:** «پنجره متن» (LLM بارگذاری‌شده) و «پنجره رسانه» (تصویر، ویدیو، صدا، لب‌خوانی). Scheduler بر اساس صف منتظر و با کمترین تعویض بین این دو جابه‌جا می‌شود.
- **تعویض LLM:** از حالت sleep در vLLM استفاده می‌شود تا وزن‌ها به RAM بروند و سریع برگردند. اگر پایدار نبود، vLLM متوقف و دوباره اجرا می‌شود. زمان تعویض در فاز ۰ اندازه‌گیری می‌شود.
- **دسته‌بندی کارها:** کارهای هم‌نوع با هم اجرا می‌شوند. مثلاً همه تصاویر منتظر، پشت سر هم با یک بار بارگذاری FLUX.
- **صف منصفانه بین مشتری‌ها:** نوبت‌دهی چرخشی (round-robin) با وزن پلن مشتری، تا یک مشتری کل GPU را اشغال نکند.
- **کارهای شبانه:** ویدیوهای سنگین (کلیپ Wan و `face`) در بازه کم‌ترافیک اجرا می‌شوند.

## ۷. ظرفیت و سهمیه (حجم تابع توانایی)

اعداد زیر **تخمین اولیه** برای 3090 Ti هستند و فقط برای برنامه‌ریزی آمده‌اند. عدد واقعی در فاز ۰ اندازه‌گیری و در سیستم ثبت می‌شود.

| کار | زمان GPU تخمینی |
|---|---|
| یک بسته متنی کامل (۶ ایجنت + بازاریابی‌سازی، یک زبان) | ۵ تا ۱۰ دقیقه |
| یک تصویر 1024px با FLUX-schnell | ۵ تا ۱۵ ثانیه |
| ویدیوی ۶۰ ثانیه‌ای حالت `voice` یا `none` (بدون کلیپ AI) | ۱ تا ۳ دقیقه |
| یک کلیپ ۵ ثانیه‌ای 720p با Wan 2.2 5B | ۱۰ تا ۱۵ دقیقه |
| ۶۰ ثانیه لب‌خوانی با LatentSync | ۵ تا ۱۰ دقیقه |

برداشت تقریبی: در ۲۰ ساعت کار روزانه GPU، حدود **۱۵ تا ۳۰ بسته کامل در روز** با ویدیوی `voice` و تصویر ساخته می‌شود. هر کلیپ Wan یا ویدیوی `face` این عدد را به‌طور محسوس کم می‌کند.

**مکانیزم سهمیه:**
1. هر مرحله زمان واقعی GPU خود را در `StepRun.gpu_seconds` ثبت می‌کند.
2. «هزینه» هر نوع بسته بر اساس میانگین متحرک این زمان‌ها حساب می‌شود.
3. ظرفیت روزانه (ثانیه GPU) بین مشتری‌ها بر اساس وزن پلن تقسیم می‌شود.
4. پنل به هر مشتری نشان می‌دهد با سهمیه امروز چند بسته از هر نوع می‌تواند بسازد. درخواست بیش از سهمیه به روز بعد می‌رود.
5. با اضافه شدن GPU در فاز ۴، ظرفیت خودکار بالا می‌رود.

## ۸. حالت‌های ویدیو

| `video_mode` | زنجیره | هزینه GPU |
|---|---|---|
| `none` | تصویر/کلیپ → متن روی تصویر → موسیقی → FFmpeg | کم |
| `voice` | TTS → Whisper (زمان‌بندی زیرنویس) → تصاویر/کلیپ‌ها → FFmpeg | کم |
| `face` | TTS یا صدای ضبط‌شده → لب‌خوانی روی عکس (SadTalker) یا ویدیوی مرجع ۳۰ تا ۶۰ ثانیه‌ای (LatentSync) → FFmpeg | بالا |

## ۹. مدل داده

- `Tenant` (پلن، وزن سهمیه)
- `User`، `Membership` (نقش)
- `Workspace` (برند)
- `BrandBrief` (نسخه‌دار)
- `Topic`
- `ContentPackage`
- `StepRun` (ایجنت، مدل، ورودی/خروجی، توکن، `gpu_seconds`)
- `Variant` (کانال، A/B)
- `MediaAsset` (پرامپت، مسیر فایل، انتخاب‌شده؟)
- `Approval` (دروازه، تصمیم، بازخورد)
- `Publication` (کانال، زمان، شناسه خارجی، UTM)
- `MetricSnapshot`
- `SpeakerProfile` (چهره، صدا، **فایل رضایت‌نامه**)
- `ChannelCredential` (رمزشده)
- `GpuQuotaLedger`

همه جدول‌ها `tenant_id` دارند.

فیلدهای Brand Brief: `brand`, `voice`, `visual_identity`, `competitors`, `personas[]`, `seed_keywords`, `pillars[]`, `ai_questions[]`, `channels[]`, `goals`, `video_mode`

## ۱۰. کانال‌ها

| کانال | اتصال | فاز |
|---|---|---|
| وردپرس | REST API + Application Password (+ فیلدهای Yoast یا Rank Math، hreflang از طریق WPML یا Polylang) | ۱ |
| تلگرام | Bot API | ۱ |
| اینستاگرام | Graph API (حساب Business) | ۲ |
| لینکدین | LinkedIn API | ۲ |
| X | API پولی ← تا فاز ۴ فقط «بسته آماده برای انتشار دستی» | ۲ (دستی) |
| یوتیوب | YouTube Data API | ۳ |
| آپارات | API آپارات (پایداری آن اول آزموده شود) | ۳ |

محدودیت‌ها و قیمت APIها مرتب عوض می‌شوند. پیش از ساخت هر کانکتور، مستندات فعلی‌اش بررسی شود.

---

# ۱۱. برنامه کامل فازها

مدت‌ها برای تیم ۲ تا ۳ نفره تخمین زده شده‌اند. هر فاز فقط وقتی تمام است که **معیارهای پذیرش** آن برقرار باشد.

## فاز ۰ — اعتبارسنجی فنی (۲ هفته)

**هدف:** قبل از ساختن محصول ثابت شود که کیفیت مدل‌ها در سه زبان قابل قبول است و 3090 Ti بار کار را تحمل می‌کند.

**کارها:**
- [ ] نصب درایور، CUDA و Docker روی سرور و راه‌اندازی MinIO، Postgres و Redis
- [ ] اجرای vLLM با Qwen3-30B-A3B (۴بیتی) و Qwen3-14B و مقایسه آن‌ها با ۵ پرامپت نمونه در هر زبان
- [ ] آزمون حالت sleep در vLLM و اندازه‌گیری زمان تعویض LLM ↔ FLUX
- [ ] نوشتن JSON Schema خروجی شش ایجنت متن و ایجنت بازاریابی
- [ ] اسکریپت پایپلاین متن از Brief تا `article.json` و تولید ۱۰ مقاله آزمایشی (fa/en/ar)
- [ ] راه‌اندازی FLUX.1-schnell روی ComfyUI و ساخت یک قالب HTML برای متن فارسی/عربی روی تصویر
- [ ] نمونه صدای Piper فارسی و Chatterbox انگلیسی/عربی و یک ویدیوی `voice` با FFmpeg
- [ ] آزمون Wan 2.2 5B و LatentSync روی 3090 Ti و ثبت زمان، VRAM و کیفیت
- [ ] پر کردن جدول زمان GPU بخش ۷ با اعداد واقعی
- [ ] پر کردن یک Brand Brief واقعی

**معیار پذیرش:**
- دست‌کم ۷ مقاله از ۱۰ مقاله هر زبان به داوری ویراستار «قابل انتشار با ویرایش جزئی» باشند
- کل زنجیره (متن + تصویر + ویدیوی `voice`) برای یک بسته بدون خطای حافظه (OOM) اجرا شود
- مدل‌های نهایی انتخاب و در مخزن داخلی ثبت شده باشند

**خروجی:** گزارش مقایسه مدل‌ها، جدول ظرفیت واقعی، اسکریپت‌های پایپلاین.

## فاز ۱ — MVP چندمشتری (۸ تا ۱۰ هفته)

**هدف:** یک جریان کامل از پرسشنامه تا انتشار و اندازه‌گیری برای ۳ تا ۵ مشتری آزمایشی.

**هفته ۱ تا ۲ — پایه:**
- [x] اسکلت FastAPI، مدل داده کامل با `tenant_id` و RLS، مهاجرت‌ها (Alembic)
- [x] احراز هویت، نقش‌ها، Workspace
- [x] Celery با صف `gpu` (تک‌نخی) و صف‌های CPU
- [x] GPU Scheduler نسخه اول (دو پنجره، دسته‌بندی کارها، ثبت `gpu_seconds`)

**هفته ۳ تا ۴ — متن:**
- [x] پرسشنامه Wizard در پنل (حدود ۲۵ سؤال، دکمه «حدس بزن»، خواندن URL سایت)
- [x] پایپلاین شش ایجنت + حلقه QA + ذخیره خروجی‌های میانی و اجرای مجدد از هر مرحله
- [x] ایجنت برنامه‌ریز تقویم موضوعی
- [x] ایجنت بازاریابی‌سازی برای وردپرس و تلگرام

**هفته ۵ تا ۶ — تایید و رسانه:**
- [x] دروازه انسانی ۱ (ویرایش مستقیم، بازگشت به مرحله دلخواه، رد با دلیل)
- [x] صف تصویر (FLUX + قالب متن HTML) — قالب HTML/Playwright واقعی است؛ خود
      وزن FLUX در فاز ۰ روی سخت‌افزار واقعی جایگزین بک‌اند شبیه‌سازی‌شده می‌شود
- [x] ویدیوی حالت `none` و `voice` (Piper، Chatterbox، Whisper، FFmpeg) —
      FFmpeg و بک‌اند پیش‌فرض espeak-ng واقعی‌اند؛ Piper/Chatterbox عمداً
      «Unavailable» تا فاز ۰ وزن‌های واقعی را جایگزین کند (`docs/voice-video.md`)
- [x] گالری انتخاب گزینه‌ها

**هفته ۷ تا ۸ — انتشار و سنجش:**
- [x] زمان‌بندی ساده (لیستی با تاریخ شمسی/میلادی)
- [x] کانکتور وردپرس (با فیلدهای سئو و اسکیما) و تلگرام
- [x] اتصال Search Console و GA4 و داشبورد پایه
- [x] سهمیه GPU هر مشتری و نمایش آن در پنل

**هفته ۹ تا ۱۰ — پایداری:**
- [x] Langfuse، لاگ‌ها، هشدار خطای انتشار (`docs/observability.md`)
- [x] پشتیبان‌گیری روزانه از Postgres و MinIO (`docs/deployment.md`)
- [x] آزمون بار: ۳ مشتری هم‌زمان — روی یک استک واقعی اجرا و تأیید شد
      (`docs/load-test.md`)
- [ ] راه‌اندازی مشتری‌های آزمایشی — تنها بند باز: نیازمند ۳ تا ۵ مشتری واقعی
      و سرور GPU واقعی است؛ کاری نیست که از این مخزن به‌تنهایی انجام شود

**معیار پذیرش:**
- هر مشتری آزمایشی بدون کمک توسعه‌دهنده دست‌کم ۱۰ بسته را از پرسشنامه تا انتشار ببرد — سنجیده می‌شود با مشتریان آزمایشی واقعی
- داده هر مشتری برای مشتری دیگر قابل مشاهده نباشد (آزمون خودکار ایزوله‌سازی) — ✅ `tests/test_tenant_isolation.py` و آزمون بار زیر بار هم‌زمان
- نرخ تایید در دروازه ۱ دست‌کم ۶۰٪ باشد — سنجیده می‌شود با مشتریان آزمایشی واقعی
- انتشار ناموفق خودکار تکرار و گزارش شود — ✅ `app/services/publishing.py` (سه تلاش، سپس `operator_alerted`)

## فاز ۲ — چندکاناله و تقویم (۶ هفته)

**هدف:** همه کانال‌های متنی و تصویری، دروازه ۲ کامل و چندزبانگی کامل.

**کارها:**
- [x] ایجنت‌های کانال اینستاگرام (کپشن، کاروسل، متن ریلز)، لینکدین و X — فیلدهای `carousel_slides`/`reel_script` گرفته و ذخیره می‌شوند؛ اتصال آن‌ها به تولید تصویر واقعی هر اسلاید و انتخاب دروازه ۲ هنوز باز است (`docs/agent-contracts.md`)
- [x] کانکتور اینستاگرام و لینکدین (HTTP واقعی، تست‌شده در برابر قرارداد مستند هر پلتفرم). برای X خروجی «بسته آماده انتشار دستی» (`docs/x-export.md`)
- [ ] تقویم کشیدن‌ورهاکردنی، منطقه زمانی هر بازار، قوانین فاصله بین پست‌ها، هشدار تعطیلات و مناسبت‌ها
- [ ] پیشنهاد زمان انتشار (اول قاعده‌محور، بعد از روی داده تعامل)
- [x] نسخه‌های زبانی فرزند یک بسته + hreflang در وردپرس (کلید متای `ai_marketing_hreflang`، بدون وابستگی به یک افزونه خاص) + ویراستار هر زبان (`docs/language-children.md`)
- [ ] نسخه‌های A/B قلاب و ثبت نتیجه
- [ ] دریافت آمار Insights از کانال‌های اجتماعی
- [x] ثبت‌نام خودکار مشتری جدید (self-service onboarding) — `POST /auth/register` با rate limit روی IP و ایمیل

**معیار پذیرش:**
- یک بسته در سه زبان و پنج کانال از یک تقویم زمان‌بندی و خودکار منتشر شود
- آمار همه کانال‌ها در داشبورد دیده شود

## فاز ۳ — ویدیو پیشرفته و یادگیری (۶ تا ۸ هفته)

**هدف:** ویدیوی AI و حالت چهره، کانال‌های ویدیویی و بسته شدن حلقه بازخورد.

**کارها:**
- [ ] کلیپ‌های Wan 2.2 5B در صف شبانه
- [ ] حالت `face`: پروفایل گوینده با بارگذاری رضایت‌نامه، SadTalker (از عکس) و LatentSync (از ویدیو)، برچسب AI
- [ ] کانکتور یوتیوب (ویدیوی بلند و Shorts) و آپارات
- [ ] ایجنت پایش GEO: مجموعه ثابت سؤال برای هر برند، بررسی دوره‌ای اشاره به برند در پاسخ‌های مدل‌های AI (از طریق جست‌وجوی وب، یا روی مدل محلی به‌عنوان تقریب)
- [ ] ایجنت تحلیلگر هفتگی: الگوهای موفق → پیشنهاد به تقویم موضوعی
- [ ] Content Refresh خودکار برای مقالات با رتبه ۸ تا ۲۰
- [ ] استفاده از بازخورد دروازه ۱ به‌عنوان نمونه در پرامپت نویسنده

**معیار پذیرش:**
- ویدیوی `face` با رضایت‌نامه ثبت‌شده ساخته و در یوتیوب منتشر شود
- گزارش هفتگی پیشنهادهای قابل اجرا تولید کند و دست‌کم یک Refresh خودکار انجام شود

## فاز ۴ — بهبود و مقیاس (پیوسته، بعد از فاز ۳)

**هدف:** اقلامی که در MVP به‌خاطر هزینه یا پیچیدگی کنار گذاشته شدند.

| حوزه | بهبود |
|---|---|
| سخت‌افزار | GPU دوم: یک کارت ثابت برای LLM و یک کارت برای رسانه، و حذف تعویض نوبتی |
| گردش کار | مهاجرت از Celery به Temporal |
| LLM | مدل بزرگ‌تر (مثلاً Qwen3-32B dense یا بالاتر) و فاین‌تیون روی محتوای تایید‌شده |
| صدای فارسی | فاین‌تیون یک TTS چندزبانه روی ManaTTS یا ParsVoice و شبیه‌سازی صدای گوینده برند |
| تصویر | LoRA اختصاصی سبک هر برند |
| ویدیو | Wan 2.2 14B یا مدل‌های قوی‌تر با GPU بیشتر |
| کانال‌ها | X API پولی |
| زیرساخت | Kubernetes، چند سرور GPU، CDN، S3 مدیریت‌شده |
| پایش | Prometheus + Grafana برای GPU، ابزارهای تجاری رتبه‌گیری |
| کسب‌وکار | پلن‌ها و صورت‌حساب مشتری بر پایه سهمیه GPU |

---

## ۱۲. سؤالات باز

- [ ] مشخصات بقیه سرور: RAM، CPU و دیسک (برای offload حداقل ۶۴ گیگ RAM و NVMe لازم است)
- [ ] مشتری‌های آزمایشی فاز ۱ چه کسانی هستند و کدام زبان‌ها را لازم دارند؟
- [ ] سرور کجا میزبانی می‌شود و آیا دسترسی پایدار به APIهای اینستاگرام، لینکدین و یوتیوب دارد؟

## ۱۳. ریسک‌های اصلی

- **یک GPU گلوگاه همه چیز است** ← Scheduler، کارهای شبانه، سهمیه، GPU دوم در فاز ۴
- **تأمین GPU و دسترسی به وزن مدل‌ها و API کانال‌ها از ایران** ← آینه داخلی و جدا کردن سرویس انتشار از سرور مدل‌ها
- **نشت داده بین مشتری‌ها** ← RLS، آزمون خودکار ایزوله‌سازی، رمزنگاری اعتبارنامه‌ها
- **محتوای انبوه و کم‌ارزش** ← دروازه انسانی، سقف تعداد انتشار، تزریق تجربه و داده اختصاصی برند
- **ادعاهای نادرست** ← برای هر آمار منبع لازم است و QA بررسی‌اش می‌کند
- **سوءاستفاده از چهره یا صدا** ← رضایت‌نامه، برچسب AI، دسترسی محدود
- **کیفیت TTS فارسی** ← مسیر جدا و فاین‌تیون در فاز ۴
